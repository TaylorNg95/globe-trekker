import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#89CFF0',
    },
    secondary: {
      main: '#FC6A03',
    },
    background: {
        default: '#B6E1F6',
    }
  },
});

export default theme;