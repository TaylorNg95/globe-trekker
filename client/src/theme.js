import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#89CFF0',
    },
    secondary: {
      main: '#FC6A03',
    },
    background: {
        default: '#CDEBF9',
    }
  },
});

export default theme;